// Tailwind CSS Configuration
// Must load synchronously after Tailwind CDN
tailwind.config = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        primary: "#111111",
        secondary: "#222222",
        dark: "#0a0a0a",
        light: "#f8fafc"
      }
    }
  }
};
